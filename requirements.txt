# yfinance: I Google Colab (nu det anbefalede kørselsmiljø, se README.md) er
# der ingen installationsbegrænsninger — brug nyeste version, den håndterer
# Yahoos anti-bot-blokering bedre:
#   pip install -q -U yfinance pandas numpy
# Kører du derimod on-device på iOS (fx a-Shell, uden kompiler til rådighed),
# så pin i stedet til 0.2.55 — verificeret på PyPI at denne (og 0.2.54-0.2.56)
# KUN kræver rene Python-pakker (ingen curl_cffi, ingen lxml):
#   yfinance==0.2.55
yfinance
pandas>=2.0
numpy>=1.24
scipy>=1.10             # find_peaks til kursmønster-detektion (cup-with-handle m.fl.,
                        # se core.py) — ren binær wheel til alle platforme, ingen
                        # kompiler krævet (i modsætning til fx TA-Lib, bevidst fravalgt)
requests>=2.28          # StockTwits-opslag (screener.py) — ren Python, ingen kompiler
streamlit>=1.30         # "Mine Aktier" GUI-app (app.py) — kun nødvendig hvis du deployer appen

# Valgfrit — kun nødvendigt for Reddit-delen af kandidat-screeneren.
# Rent Python (ingen kompilerede afhængigheder), men Reddit-sentiment
# udelades automatisk (uden fejl) hvis den ikke er installeret eller
# API-nøgler ikke er sat op — se README.md "Kandidat-screener".
praw>=7.7

# --- Saxo Bank-integration (trading/, se SAXO_INTEGRATION.md) -----------
# sqlite3 og dataclasses er indbygget i Python — ingen nye afhængigheder
# for Lag 1+2 (signal_job.py, trading/decision.py). "requests" ovenfor
# dækker også Lag 3's OAuth/REST-kald mod Saxo.
pynacl>=1.5             # kryptering af OAuth-tokens til GitHub Actions-secrets
                        # (trading/token_store.GitHubSecretTokenStore) — bruges når
                        # Lag 3 kører helt via GitHub Actions uden VPS, se
                        # SAXO_INTEGRATION.md, "Hosting via GitHub Actions"

# websocket-client — kun nødvendig HVIS/NÅR du selv færdiggør
# trading/monitor.py's run_websocket_forever() (kræver en vedvarende VPS,
# se SAXO_INTEGRATION.md). Afkommentér når den dag kommer:
# websocket-client>=1.7
